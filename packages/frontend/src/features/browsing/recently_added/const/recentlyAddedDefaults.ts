// MAX_DAYS bounds the progressive load for libraries holding songs with no
// known `added` time, which the server-side filter can never return.
export const RECENTLY_ADDED_STEP_DAYS = 30;
export const RECENTLY_ADDED_MAX_DAYS = 36500;
